export const COVERAGE_TYPES = {
  SINGLE: 'SINGLE_TEACHER_ALL_PERIODS',
  PER_PERIOD: 'PER_PERIOD'
};

export const DEFAULT_COVERAGE_TYPE = COVERAGE_TYPES.SINGLE;

export const DEFAULT_NOTES = {
  principalNote: '',
  materialsLink: ''
};